set(hyper_res).
clear(print_back_sub).
assign(max_weight,7).
assign(stats_level,1).

lex([*,+,<=,x,y,z,u,a,b,c]).

list(sos).
x <= x.
-(x <= y) | -(y <= x) | (x=y).
-(x <= y) | -(y <= z) | (x <= z).

(x*y) <= x.
(x*y) <= y.

-(u <= x) | -(u <= y) | (u <= x*y). 

x <= (x+y).
y <= (x+y).

-(x <= u) | -(y <= u) | (x+y <= u).

end_of_list.

list(passive).
-((a+ (b*c) <= (a+b)* (a+c))).
end_of_list.