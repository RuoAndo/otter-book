set(knuth_bendix).

list(usable).
	(x+y)+z=x+y+z.
	0+x=x.
	(-x)+x=0.
	x+y=y+x.
	(x*y)*z=x*y*z.
	x* (y+z)= (x*y)+ (x*z).
	(x+y)*z= (x*z)+ (y*z).
end_of_list.

list(sos).
	x*x=x.
end_of_list.

list(passive).
	x=x.
	a*b!=b*a.
end_of_list.
